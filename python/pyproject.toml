[project]
name = "build-pipeline-refactoring-kata"
version = "0.1.0"
description = "Build Pipeline Refactoring Kata in Python"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "pytest",
    "approvaltests",
    "coverage",
]

